# Drift Ledger

Known disagreements between the code and its prose layers (Pitch/Pledge/Plan/Pin/Priority, summaries, comments), plus deferred TODO items. Per the 5P Protocol (`docs/MODULE_DESCRIPTIONS.md`), reconciling drift is a **fix**-category change agreed in prose first; until then it is recorded here so it isn't silently re-discovered or, worse, "corrected" in the wrong direction. Remove an entry in the same change that reconciles it.

_All items surfaced during the initial library-wide documentation pass (2026-07) have been reconciled.  Add new drift below as it is found._

## Likely bugs (code probably wrong)

## Convention violations (project rules)

## Doc-only corrections (summaries/comments wrong; code fine)

## Semantic decisions needed (which side is right is unclear)
