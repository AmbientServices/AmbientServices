# Glossary — AmbientServices

Authoritative definitions of the project's invented vocabulary, so every collaborator (human or AI) reads the same meaning into the prose descriptions. Maintained per the model in `docs/MODULE_DESCRIPTIONS.md`. Add an entry whenever a term is coined; keep definitions terse.

